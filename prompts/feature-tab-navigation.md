# Feature: Bottom Tab Navigation

The product team wants to add bottom tab navigation to the app.

The app should have three tabs: Home, Explore, and Profile. Each tab should display its
own distinct screen with placeholder content. The active tab should be visually highlighted.
Switching tabs should preserve the scroll position and state of each screen independently —
navigating away from a tab and returning should not reset its content.
