# Feature: Multi-Counter

The product team wants to build a multi-counter feature.

Users should see a list of named counters. Each counter should display its current count
and have increment and decrement buttons. The count should not go below zero. Users should
be able to reset any individual counter back to zero. The total sum of all counters should
be displayed at the top of the screen.
