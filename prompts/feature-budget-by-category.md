# Feature: Budget by Category

The product team wants to build a budget tracking feature grouped by category.

Users should be able to add expenses with an amount and a category (e.g. Food, Transport,
Entertainment, Other). The screen should display each category with the total spent and
the number of transactions in that category. A grand total across all categories should
appear at the top. Users should be able to filter the list to show only a selected category.
