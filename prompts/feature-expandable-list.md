# Feature: Expandable List

The product team wants to build an expandable list feature.

Users should see a list of category headers. Tapping a header should expand it to reveal
the items within that category; tapping again should collapse it. Only one category should
be expanded at a time — opening a new category should automatically close the previously
open one. The expand and collapse action should be animated smoothly.
