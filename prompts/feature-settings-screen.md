# Feature: Settings Screen

The product team wants to build a settings screen.

Users should be able to toggle dark mode on and off. They should also be able to select
a preferred language from a list of options and set a daily reminder time using a time
picker. All preferences should persist across app restarts. The current value of each
setting should be clearly visible on the settings screen without requiring the user to
open a sub-menu.
