# Feature: Image Gallery

The product team wants to build an image gallery feature.

Users should see a grid of image thumbnails loaded from a provided list of URLs. Tapping
a thumbnail should open a full-screen detail view of that image. The detail view should
support a back action to return to the grid. Images should display a loading placeholder
while they are being fetched and an error placeholder if loading fails.
